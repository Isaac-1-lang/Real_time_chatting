{
  "name": "project_9",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "node -v npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node -v npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "path-to-regexp": "^8.2.0"
  }
}
